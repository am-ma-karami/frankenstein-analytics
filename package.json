{
  "name": "frankenstein-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "User Analytics Dashboard — take-home integration task",
  "scripts": {
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix frontend test"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
